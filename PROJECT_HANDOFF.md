# تحویل کامل پروژه Social Verse Creator Intelligence

این فایل مرجع انتقال پروژه به Claude، Codex یا هر توسعه‌دهندهٔ بعدی است. برای درک دقیق وضعیت پروژه، ابتدا این فایل و سپس `README.md` خوانده شود.

## ۱. هدف محصول

ساخت یک بات خبری و آموزشی برای کامیونیتی **Social Verse** که هر روز خبرهای مهم حوزهٔ تولید محتوا و اقتصاد کریتورها را جمع‌آوری کند، موارد تکراری را حذف کند، آن‌ها را به فارسی محاوره‌ای و کاربردی بازنویسی کند و داخل تاپیک تلگرام منتشر کند.

نام بات: **Social Verse Creator Intelligence**

نام کاربری بات: `@SocialVerseCreatorIntelBot`

گروه مقصد: **Social Verse**

تاپیک مقصد: **📡 رادار محتوا 📡**

## ۲. تصمیم‌های قطعی محصول

- زبان خبرها و آموزش‌ها فارسی، ساده و محاوره‌ای است.
- خروجی فقط خلاصهٔ خبر نیست؛ باید توضیح دهد چه اتفاقی افتاده، چرا برای کریتور مهم است و چه اقدام عملی‌ای پیشنهاد می‌شود.
- نام منبع و لینک خبر در پیام منتشرشده نمایش داده نمی‌شود.
- URL خبر فقط داخل سیستم و برای جلوگیری از انتشار تکراری نگهداری می‌شود.
- پیام‌های خودکار بی‌صدا ارسال می‌شوند تا شب یا ساعات استراحت مزاحمت ایجاد نکنند.
- تاپیک رادار بسته شده است؛ فقط بات و ادمین‌هایی که دسترسی **Manage Topics** دارند می‌توانند در آن پیام بگذارند.
- اجرای سیستم نباید به روشن‌بودن کامپیوتر شخصی وابسته باشد.
- هستهٔ سیستم باید دائماً بدون API پولی، اعتبار آزمایشی یا کارت بانکی کار کند.
- Gemini فقط برای بالابردن کیفیت است و جزء ضروری سیستم نیست.

## ۳. معماری

```text
RSS feeds
   ↓
Collector (دریافت و نرمال‌سازی)
   ↓
Freshness + duplicate filters
   ↓
Gemini quality layer (اختیاری)
   ↓ خطا، نبود کلید یا اتمام سهمیه
Local rule-based analyzer (همیشه در دسترس)
   ↓
Telegram Forum Topic (silent)
   ↓
Commit publication history to GitHub
```

اجزای اصلی:

- `src/social_verse/config.py`: خواندن تنظیمات محیطی و اعتبارسنجی
- `src/social_verse/collector.py`: دریافت RSSها و تبدیل آن‌ها به مدل داخلی
- `src/social_verse/database.py`: تاریخچه و تشخیص موارد تکراری
- `src/social_verse/analyzer.py`: تحلیل محلی و لایهٔ اختیاری Gemini
- `src/social_verse/publisher.py`: ارسال پیام بی‌صدا به Telegram Bot API
- `src/social_verse/main.py`: هماهنگ‌کنندهٔ کل فرایند
- `config/feeds.json`: فهرست منابع RSS
- `data/published_news.json`: حافظهٔ خبرهای منتشرشده

منابع فعلی RSS:

- Meta Newsroom
- YouTube Blog
- OpenAI News
- Google AI Blog

خرابی یک feed نباید کل اجرا را متوقف کند.

## ۴. زمان‌بندی و اجرا

Workflow اصلی: `.github/workflows/daily_news.yml`

نام آن در GitHub Actions: **Creator Intelligence Daily News**

- Cron فعلی: `0 7 * * *`
- زمان عادی اجرا: هر روز ساعت **۱۰:۳۰ صبح به وقت ایران** (07:00 UTC)
- اجرای زمان‌بندی‌شده حداکثر ۵ مورد منتشر می‌کند.
- اجرای دستی به‌صورت پیش‌فرض حداکثر ۱ مورد منتشر می‌کند.
- GitHub ممکن است اجرای cron را چند دقیقه دیرتر شروع کند.

Workflow پس از انتشار موفق، `data/published_news.json` را commit می‌کند تا اجرای بعدی حافظه داشته باشد.

## ۵. هوش مصنوعی و کیفیت متن

مدل پیش‌فرض Gemini: `gemini-3.1-flash-lite`

Gemini باید خروجی ساختاریافته‌ای با این مفهوم بسازد:

1. عنوان کوتاه و جذاب
2. چه اتفاقی افتاده؟
3. چرا برای کریتورها مهم است؟
4. حالا چه کاری انجام بدهیم؟

لحن باید طبیعی و محاوره‌ای باشد، نه ترجمهٔ ماشینی و نه متن رسمی روابط عمومی. آموزش‌ها باید کوتاه، قابل‌اجرا و مرحله‌به‌مرحله باشند. هیچ نام منبع یا URL نباید در متن نهایی بیاید.

اگر Gemini پاسخ نامعتبر بدهد، سهمیه تمام شود، کلید وجود نداشته باشد یا API خطا بدهد، سیستم بدون توقف از `LocalAnalyzer` استفاده می‌کند.

## ۶. سیاست صفرهزینه

- اجرای ابری با GitHub Actions روی Repository عمومی انجام می‌شود.
- هیچ Billing یا روش پرداختی برای Gemini فعال نشود.
- Free Tier سرویس ثالث تضمین دائمی ندارد؛ به همین دلیل Gemini اختیاری است.
- مسیر پایه شامل RSS، تحلیل محلی، GitHub Actions و Telegram است و به سرویس AI پولی وابسته نیست.
- هر قابلیت جدید باید یا رایگانِ بدون محدودیت زمانی باشد، یا یک fallback رایگان و مستقل داشته باشد.
- راه‌حل نباید به روشن‌ماندن لپ‌تاپ یا اجرای سرور شخصی وابسته باشد.

## ۷. Secretها و امنیت

این نام‌ها در **GitHub Actions Secrets** استفاده می‌شوند:

- `TELEGRAM_BOT_TOKEN`
- `TELEGRAM_CHAT_ID`
- `TELEGRAM_TOPIC_ID`
- `GEMINI_API_KEY` — اختیاری

متغیر اختیاری Repository:

- `GEMINI_MODEL`

مقدار هیچ Secretی نباید در کد، لاگ، issue، prompt، screenshot یا این سند نوشته شود. برای انتقال پروژه فقط نام Secretها منتقل می‌شود؛ مقادیر همچنان در GitHub باقی می‌مانند.

## ۸. Workflowهای عملیاتی

- `daily_news.yml`: اجرای اصلی روزانه یا دستی
- `telegram_test.yml`: تست اتصال و ارسال پیام بی‌صدا به تاپیک
- `gemini_test.yml`: بررسی اتصال Gemini و اعتبار مدل/کلید
- `lock_topic.yml`: بستن تاپیک برای جلوگیری از ارسال پیام توسط اعضای عادی

ترتیب بررسی در زمان خطا:

1. صفحهٔ Run و مرحلهٔ قرمز workflow را باز کنید.
2. بررسی کنید Secret موردنیاز وجود دارد؛ مقدار آن را در لاگ چاپ نکنید.
3. اگر Gemini خطا دارد، مطمئن شوید اجرای اصلی با fallback محلی ادامه پیدا می‌کند.
4. اگر Telegram خطا دارد، دسترسی ادمین بات، شناسهٔ گروه و شناسهٔ تاپیک را بررسی کنید.
5. اگر خبر تکراری منتشر می‌شود، `data/published_news.json` و منطق تطبیق URL/عنوان را بررسی کنید.

## ۹. وضعیت فعلی

- Repository اصلی ساخته شده و روی شاخهٔ `main` است.
- اتصال Telegram با پیام تست واقعی تأیید شده است.
- بات در گروه ادمین است و در تاپیک رادار پیام می‌فرستد.
- ارسال بی‌صدا فعال است.
- قفل تاپیک رادار فعال شده است.
- اتصال Gemini و مدل فعلی در معماری قرار دارد و fallback محلی حفظ شده است.
- حذف نام و لینک منبع از پیام‌های نهایی اعمال شده است.
- اولین مسیر انتشار واقعی و ثبت تاریخچه راه‌اندازی شده است.

## ۱۰. اجرای محلی امن

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
PYTHONPATH=src python -m social_verse.main
```

تنظیم پیش‌فرض `.env.example` حالت `DRY_RUN=true` دارد و نباید پیام واقعی بفرستد.

## ۱۱. کارهای فاز بعد

- بهبود کیفیت فیلتر خبر و امتیازدهی موضوعی
- تولید آموزش‌های مستقل و کاربردی برای کریتورها
- Viral Radar
- دستیار ایده و برنامه‌ریزی محتوا
- گزارش هفتگی
- پایش شکست workflow بدون اضافه‌کردن سرویس پولی

هر قابلیت جدید باید اصول صفرهزینه، فارسی محاوره‌ای، عدم نمایش منبع، ارسال بی‌صدا و fallback مستقل را حفظ کند.

## ۱۲. روش انتقال به Claude

Repository عمومی:

`https://github.com/JamzStudio/social-verse-creator-intelligence`

در شروع گفتگو با Claude، لینک Repository را بدهید و صریحاً بخواهید قبل از هر اقدامی فایل‌های `CLAUDE.md`، `PROJECT_HANDOFF.md` و `README.md` را کامل بخواند. اگر محیط Claude دسترسی مستقیم به GitHub ندارد، همین سه فایل به‌همراه پوشه‌های `src`، `.github/workflows` و `config` آپلود شوند.

برای تغییر کد، Claude به دسترسی GitHub نیاز دارد؛ برای مشاوره یا بررسی معماری، Repository عمومی یا فایل ZIP کافی است. مقدار Secretها را هرگز در چت Claude وارد نکنید.
